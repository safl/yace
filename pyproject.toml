[build-system]
requires = [
    "setuptools>=54",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.ruff]
select = ["E", "F", "I"]
per-file-ignores = {"src/yace/idl/datatypes.py" = ["E742"]}
