const char *
{{ meta.prefix }}_{{ topic.symbol }}_str(enum {{ meta.prefix }}_{{ topic.symbol }} eval)
{
	switch (eval) {
{%- for entity in topic.members %}
	case {{ meta.prefix | upper }}_{{ topic.symbol | upper }}_{{ entity.symbol | upper }}:
		return "{{ topic.symbol | upper }}_{{ entity.symbol | upper }}";
{%- endfor %}
	}

	return "ENOSYS";
};

