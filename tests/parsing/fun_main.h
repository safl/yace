/**
 * Print hello world
 *
 * @param argc Number of command-line arguments
 * @param argv Number of command-line arguments
 *
 * @return On success, 0 is returned. On error, -1 is returned and errno set to indicate the error
 */
int
main(int argc, const char **argv);

int ***
baz(void);

struct jazz
bar(void);

const char *
foo(void);